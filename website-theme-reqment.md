I will provide a design prompt.

Your task is to analyze the prompt and convert it into a production-ready
responsive Next.js component using TailwindCSS and Hero UI.

IMPORTANT RULES:

1. Follow the exact layout structure described in the prompt.
2. Maintain proper spacing, hierarchy, alignment, and component positioning.
3. Generate clean, reusable, production-ready code.
4. Create components that can easily be integrated into a Next.js project.
5. Do not generate unnecessary sections.
6. Generate only the component requested.
7. Maintain consistency across all generated components.

COLOR SYSTEM:

Primary Light: #C6F4D6 Primary Soft: #8FE3B0 Primary Muted: #4AD27A Primary:
#22C55E Primary Dark: #16A34A Primary Darker: #15803D

Neutral Colors: White: #FFFFFF Surface: #F8FAFC Border: #E2E8F0 Text Secondary:
#64748B Text Primary: #1E293B Dark Background: #0F172A

COLOR RULES:

- Never generate random colors.
- Never introduce new colors.
- Use only the color palette above.
- Maintain strict color consistency.
- Hero sections should use gradients based on the provided green palette.
- Primary buttons must use Primary and Primary Dark.
- Cards must use White backgrounds with Border color.
- Badges should use Primary Soft.
- Dashboard sidebar should use Primary Darker.
- Hover states should use Primary Soft.
- Light mode and dark mode must follow the same design language.
- Maintain accessibility and readability.

DESIGN RULES:

- Premium SaaS quality.
- Modern UI.
- Human-friendly design.
- Recruiter-attractive design.
- Clean spacing.
- Consistent typography.
- Consistent card design.
- Natural green branding.
- Elegant and minimal design.
- Fully responsive.
- Mobile-first approach.

AVOID:

- Generic templates.
- Bootstrap-looking UI.
- Outdated layouts.
- Random colors.
- Neon colors.
- Gaming UI.
- Inconsistent spacing.

COMPONENT RULES:

- Fully responsive.
- Reusable.
- Production-ready.
- Next.js friendly.
- Hero UI compatible.
- Clean component structure.

Use:

- rounded-3xl
- smooth transitions
- subtle shadows
- modern hover effects
- proper spacing
- consistent typography

LAYOUT RULES:

Always use:

container mx-auto

Never use:

max-w-xs max-w-sm max-w-md max-w-lg max-w-xl max-w-2xl max-w-3xl max-w-4xl
max-w-5xl max-w-6xl max-w-7xl

Use responsive grid and flex layouts.

TYPOGRAPHY RULES:

- font-semibold
- font-bold
- tracking-tight
- leading-relaxed

Create strong visual hierarchy.

OUTPUT RULES:

- Card → Return only the card component.
- Hero Section → Return only the hero section.
- Navbar → Return only the navbar.
- Footer → Return only the footer.
- Dashboard Widget → Return only the widget.
- Modal → Return only the modal.
- Form → Return only the form.

Do not generate a complete website unless explicitly requested.

CODE RULES:

- Use Next.js Component.
- Use TailwindCSS.
- Use Hero UI Components when appropriate.
- Use semantic HTML.
- Keep code clean and readable.

OUTPUT FORMAT:

Return code only.

Do not explain anything.

Do not add markdown explanations.

Do not add unnecessary comments.

Return a single reusable production-ready component.
